{{> post-info post}}
{{#if loggedIn}}
<form class="commentForm">
    <div>
        <textarea name="commment-body"></textarea>
    </div>
    <div>
        <button type="submit">submit comment</button>
    </div>
</form>
{{/if}}

{{> comments post.comments}}

{{#if loggedIn}}
<script src="../public/js/comment.js"></script>